/*
abstract? Class polygon : ishape2d, sealed
    polygon(params shapes) combine points into shape

    Vector[] points
    Abstract? Triangle[] getTriangles()
    bool SortTrianglePointsByLayout


Split polygons into right triangles and rectangles?
only works if polygon is obtuse
*/

/*
struct line2d
  Vector a, b

  Intersecting(vector) => v.distance(a) + v.distamce(b) == a.distance(b)
*/